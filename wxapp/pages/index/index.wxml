<view class="container">
  <view class="status-box {{isAlarm ? 'alarm' : 'normal'}}">
    <text class="status-text">{{statusText}}</text>
  </view>
  
  <view class="info" wx:if="{{isAlarm}}">
    <text>检测目标: {{targetName}}</text>
    <text>置信度: {{confidence}}</text>
  </view>

  <button class="btn" bindtap="connectSocket">重连服务器</button>
</view>

<button class="btn" bindtap="connectSocket">重连服务器</button>

<view class="log-container">
  <view class="log-title">🛡️ 报警历史 (最近50条)</view>
  <scroll-view scroll-y="true" class="log-list">
    <block wx:for="{{historyLogs}}" wx:key="id">
      <view class="log-item">
        <text class="log-time">{{item.shortTime}}</text>
        <text class="log-obj">{{item.object_class}}</text>
        <text class="log-conf">置信度: {{item.confidence}}</text>
      </view>
    </block>
  </scroll-view>
</view>