# 🏭 Factory Vision 2.0

> 基于 YOLOv8 与 FastAPI 构建的工业级视觉检测微服务系统。
> **Industrial Object Detection Microservice based on YOLOv8 & FastAPI.**

---

## 📖 项目简介 (Introduction)

**Factory Vision** 是一个针对工业场景（如耳机生产线）设计的视觉检测系统。
v2.0 版本进行了全面的架构重构，从单机脚本升级为 **Client-Server 微服务架构**，实现了算法核心与业务逻辑的解耦，支持高并发、接口化调用。

### 🌟 核心特性 (Key Features)
* **微服务架构**：基于 **FastAPI** 构建 RESTful API，支持异步处理。
* **核心解耦**：`App` 层与 `Core` 层分离，算法引擎 (`AIEngine`) 独立封装，易于更换模型。
* **实战级模型**：针对复杂背景、遮挡、逆光场景进行了混合数据训练与负样本清洗。
* **自动化质检**：提供 Python 客户端脚本，支持批量上传与结果统计。

---

## 🏗️ 目录结构 (Directory Structure)

```text
Factory_Vision_2.0/
├── datasets/            # 训练数据集 (Images & Labels)
├── runs/                # YOLO 训练日志与模型权重
├── src/
│   ├── app/             # [服务层] FastAPI 接口入口
│   │   └── main.py      # 启动文件
│   ├── core/            # [核心层] AI 引擎封装
│   │   └── engine.py    # YOLO 模型加载与推理逻辑
│   ├── training/        # [训练层] 模型训练脚本
│   │   └── train.py
│   ├── client/          # [客户端] 测试与批量处理脚本
│   │   └── client.py
│   └── playground/      # [实验区] 历史代码与实验 Demo
├── requirements.txt     # 项目依赖库
└── README.md            # 项目文档

```

---

## ⚡ 快速开始 (Quick Start)

### 1. 环境准备

确保已安装 Python 3.10+。建议使用虚拟环境。

```bash
# 安装依赖
pip install -r requirements.txt

```

*(如果没有 requirements.txt，请手动安装核心库：`pip install ultralytics fastapi uvicorn python-multipart requests opencv-python`)*

### 2. 启动服务端 (Server)

在项目根目录下运行：

```bash
uvicorn src.app.main:app --reload

```

* 服务默认运行在：`http://127.0.0.1:8000`
* API 文档地址：`http://127.0.0.1:8000/docs`

### 3. 运行客户端 (Client)

打开一个新的终端窗口，运行测试脚本：

```bash
python src/client/client.py

```

---

## 🛠️ 开发与维护 (Development)

### 模型训练

如需重新训练模型（例如加入新数据），请运行：

```bash
python src/training/train.py

```

*训练生成的新模型位于 `runs/detect/trainX/weights/best.pt*`

### 配置修改

* **模型路径**：在 `src/core/engine.py` 中修改 `MODEL_PATH`。
* **阈值调整**：在 `src/app/main.py` 或 `src/client/client.py` 中调整 `conf` 参数。

---

## 📝 版本历史 (Changelog)

* **v2.0 (Current)**: 架构重构。引入 FastAPI，分离 App/Core 层，增加负样本训练，修复误检问题。
* **v1.0**: 初始版本。基于 YOLOv8n 的单机脚本，完成基础数据采集与模型验证。

---

> Author: LJR | Tech Stack: Python, YOLOv8, FastAPI, OpenCV

```

---

### 💡 最后一个专业细节：生成 `requirements.txt`

README 里提到了 `requirements.txt`，这是一个标准项目的标配。

请在终端里运行这一行命令，自动生成它：

```bash
pip freeze > requirements.txt

```